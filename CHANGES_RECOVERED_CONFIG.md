# Recovered configuration — September 6, 2026

- Added five user-supplied files unchanged: map PGM/YAML, waypoint YAML, localization launch and localization parameters.
- Added a minimal ament_cmake package retaining the required turtlebot3_hmi_social name; no social policy code included.
- Added isolated overlay build instructions and checker notice for the original false validation marker.
- Preserved all controller logic and coordinates. Startup pose and A_START differ; README explains existing trial reset behavior.
- Static validation only; ROS/robot execution has not been performed. This is not proof of the final experimental version.

Earlier CHANGES_RUNTIME.md documents the preceding packaging revision; its missing-file statements describe that earlier state.
